<h1> All Reservations </h1>

<% @reservations.each do |reservation| %>

<%= reservation.time  %>
<%= reservation.party_size  %>
<%= reservation.customer_id %>
# include user.name, phone number etc.
<%= link_to 'Delete Reservation', restaurant_reservation_path(reservation.restaurant, reservation), method: :delete, data: {confirm: 'Are you sure you want to delete this reservation?'} %>

<%end%>
